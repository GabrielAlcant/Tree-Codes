# 🌳 Árvore Rubro-Negra (Red-Black Tree) Interativa

Uma implementação robusta e interativa de uma **Árvore Rubro-Negra** em Python. Este projeto oferece uma interface via linha de comando (CLI) para manipular a árvore, permitindo inserção, remoção, busca e visualização hierárquica, mantendo o balanceamento automático.

## 📋 Funcionalidades

O programa atende aos 4 requisitos principais de manipulação de dados:

1.  **Inserção Balanceada:** Adiciona elementos e corrige automaticamente violações de cor e altura usando rotações (esquerda/direita).
2.  **Remoção Segura:** Remove nós (folhas ou com filhos) e rebalancea a árvore para manter as propriedades Rubro-Negras.
3.  **Pesquisa:** Busca por valores e retorna informações sobre o nó (valor, cor e nó pai).
4.  **Visualização:** Exibe a estrutura da árvore no terminal indicando hierarquia (R----/L----) e cores.

## 🚀 Como Executar

### Pré-requisitos
* Python 3.x instalado.
* Nenhuma biblioteca externa é necessária (apenas bibliotecas padrão).

### Passo a Passo

1.  Clone este repositório ou baixe o arquivo de código.
2.  Salve o código principal em um arquivo, por exemplo: `rbtree.py`.
3.  Abra o terminal na pasta do arquivo.
4.  Execute o comando:

```bash
python Black-Red.py
